// Legacy placeholder removed: real OG image route lives under
// `opengraph-image/route.js`. Keep a benign default export here so
// tooling that still references this file won't fail during builds.
export default function Page() {
  return null;
}