import React from 'react';

export const metadata = {
  title: 'Explore Tags | StoryVermo',
  description: 'Discover popular and trending tags on StoryVermo. Explore stories by tag and find content that interests you.',
};

export default function TagsLayout({ children }) {
  return children;
}
